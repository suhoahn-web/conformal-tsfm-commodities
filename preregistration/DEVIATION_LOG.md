# Deviation log

Every departure from the frozen pre-registration in `PREREGISTRATION.md`, with the
date it was made and the reason. Entries added after a result was seen are marked as
such. Corrections to earlier entries are appended in place rather than overwritten.

| Date | Change | Reason |
|---|---|---|
| 2026-08-11 | Rolling → recursive evaluation scheme; metrics set expanded (rMAE, PT test, GW test, recursive MSPE paths, crisis-excluded audit) | AKV 2013 + Lago 2021 protocol extraction (close reading) — plan not yet frozen |
| 2026-08-11 | Headline claim qualifiers made load-bearing per prior-art close reading | notes: achour/ma/finverse/wang_zhang/fedchronos |
| 2026-08-11 | Smoke A+B passed. Finding logged: Chronos-Bolt clamps quantiles to [0.1,0.9] — native 95% intervals impossible for Bolt family; concrete motivation for conformal layer. AR baseline to be re-specified on returns (WTI asinh-level AR distorted by 2020 negative price). | analysis/smoke_a, analysis/smoke_b |
| 2026-08-12 | **Three code defects found by adversarial internal review and fixed; all results regenerated.** (a) *Look-ahead in conformal calibration*: calibration sets were indexed by target date, so for h>1 they included outcomes realised after the forecast origin. Fixed by shifting the calibration window back by the horizon (`conformal.py:_cal_end`), with ACI's online alpha feedback likewise delayed; enforced by `src/eval_core/test_no_leakage.py` (9/9 pass). (b) *CRPS not comparable across models*: the quantile-grid approximation was computed on each model's own grid, so the score tracked grid size (5 levels for clamped Bolt, 9 for TimesFM, 7 for the rest) rather than distributional quality. Fixed by restricting to the common grid {0.10,0.50,0.90}. *[Correction 2026-08-12: this line originally recorded the grid as {0.10,0.25,0.50,0.75,0.90}, which was never implemented — `analysis/phase1_local/recompute_crps.py:26` uses {0.10,0.50,0.90}, the true intersection, because TimesFM 2.5 emits deciles and Chronos-Bolt is clamped to [0.1,0.9]. The reported numbers were always computed on the narrower grid; only this log line was wrong. Corrected rather than silently overwritten.]* (c) *LEAR-lite numerical blow-up*: an extrapolated LASSO forecast passed through sinh() produced MSPE ratios up to 1.2e13 on WTI, corrupting its MCS entry. Fixed with a training-window-derived clip on the cumulated asinh move. **The Hormuz holdout is re-evaluated after these fixes.** The fixes were identified from the code, not from holdout values; they change all windows identically; the pre-committed interpretations in HORMUZ_PRECOMMIT.md are unchanged and were written before either evaluation. | reviews: `docs/review_R2_ml_and_fit.md` |
| 2026-08-12 | **Four further deviations logged retrospectively after review.** (a) The headline panel was specified as 6 series (energy + metals) but all 10 series are reported as the headline, with agriculture no longer demoted to robustness — the broader panel is more conservative for our negative conclusion, but the change was unlogged. (b) Clark–West tests for nested comparisons were specified in §3.2 and not run; DM with HLN correction and GW were used instead. (c) The futures-curve benchmark named in §3.3 was not constructed; only the no-change benchmark is used. (d) Christoffersen independence statistics are computed in `metrics.var_backtest` but only Kupiec results are reported. Each is either reported in the manuscript's limitations or reinstated before submission. | review: `docs/review_R1_econometrics.md` |
| 2026-08-12 | Time-MoE and TTM, listed in the frozen §3.3 roster, were not run (weights not transferred to the server) and must be reported as not evaluated rather than silently omitted. | plan §3.3 vs manuscript §4.4 |
| 2026-08-11 | Hormuz unsealing: first launch of `run_hormuz_final.py` was killed by the tool harness before completion — **no output file was written, no marker was created, and no result was observed**. Re-launched as a detached process. This does not constitute a second evaluation pass; the single-use marker mechanism remains intact and untriggered. | approval file created 2026-08-11 recording the user's verbal approval |
| 2026-08-12 | **Directional-accuracy test is the Pesaran–Timmermann (1992) statistic, not the serial-correlation-robust (2009) version the AKV protocol uses.** Discovered while verifying implementations against the newly obtained primary sources: `metrics.pesaran_timmermann` implements the 1992 variance expression term for term. Since our forecast origins overlap at h = 5 and h = 22, the statistic is anti-conservative at those horizons. Not re-run, because the directional results are at chance level (success ratios 0.462–0.546, median 0.502) and no correction can make those significant; the limitation is now stated in manuscript §4.7 instead. | primary source: Pesaran & Timmermann (1992), JBES 10(4), pp. 461–462 |
| 2026-08-12 | **Reference substitutions made after the download round, each to avoid citing a source we cannot read or a method we do not implement.** (a) Winkler (1972) → Gneiting & Raftery (2007) §6.2, which defines the interval score we compute. (b) Künsch (1989) → Hansen, Lunde & Nason (2011) p. 468, which specifies the moving-block bootstrap we use. (c) Koenker & Xiao (2006) → Koenker & Bassett (1978), because QR-AR is quantile regression on AR lags, not the random-coefficient QAR model. (d) Diebold & Mariano: the artefact obtained is the 2002 JBES reprint; we cite the 1995 original (corroborated in HLN p. 465) and note the reprint. Harvey, Leybourne & Newbold (1997) remains the one cited work with no PDF; its formula was verified against a printed statement of it and matches our code. | `docs/references_v2.md` |
| 2026-08-12 | **Implementations verified against primary sources now held.** MCS elimination rule confirmed against HLN p. 466 (`e_R,M = argmax_i sup_j t_ij`, signed — the 2026-08-12 fix is correct); Christoffersen LR_ind confirmed against IER pp. 845–846; PT variance confirmed against JBES pp. 461–462; HLN small-sample correction confirmed against a printed statement of the formula. No further code changes were required. | `docs/references_v2.md` §A.1–A.2 |
| 2026-08-13 | **Post-hoc robustness sweep (E3/E4) shows the pre-registered framing overstated the crisis-calibration result; claims narrowed.** An adversarial pre-submission review asked whether the split-conformal failure was a property of non-adaptive calibration or of our 250-observation window. We swept trailing windows {50,100,250,500} and ACI step sizes {0.005,0.01,0.02,0.05} on the immutable cache (`analysis/phase3_sensitivity/`), with the interpretation of each possible outcome written down **before the sweep finished** (`PRECOMMIT_E3.md`). Outcome B obtained at h=1 and h=5: a 50-observation window recovers most of the crisis coverage (0.800/0.813 vs 0.733/0.656 at h=1) and beats ACI outright at h=1 in COVID (0.800 vs 0.770). Consequently: (a) "ACI is the best construction at every horizon and regime" is **withdrawn as false**; (b) "static split conformal is worse than no calibration" is narrowed to a statement about the 250-observation window; (c) the construction is renamed **Rolling-SC(w)** because its calibration window rolls — our own code docstring already said so; (d) the width cost is corrected from "+25%" (h=5 calm) to **+44% at h=22**; (e) the pre-registered gamma=0.02 is reported as **not the best value** (gamma=0.05 dominates it in every crisis cell) and is nonetheless retained as the headline configuration. The surviving claim is stability, not dominance: worst-case coverage deviation 0.044 for a single ACI setting vs 0.121 for the best fixed window. New §5.6a and Table 15. | `analysis/phase3_sensitivity/PRECOMMIT_E3.md`, `results_sensitivity.md` |
| 2026-08-13 | **Value-at-risk backtests moved to non-overlapping origins (E2); the panel-wide h=22 failure was an overlap artefact.** At h=5 and h=22 forecasts are issued daily, so the exceedance sequence is mechanically dependent and the Kupiec/Christoffersen assumptions fail. Re-running on a thinned sample (every h-th origin) removes the apparent failure for every model except full fine-tuning: rejection share at h=22 falls from 0.6–0.8 to 0.0–0.2. §5.9 rewritten. The fine-tuning result strengthens and sharpens — median lower-tail exceedance 0.090/0.096/0.094 against nominal 0.05, the only model rejected on a majority of series at every horizon on valid samples. Christoffersen independence statistics are now reported as descriptive only at h>1. New Table 16. | `analysis/phase3_sensitivity/results_var_nonoverlap.md` |
| 2026-08-13 | **Further corrections from the same review, all text-level.** (a) The "identical information set" fairness claim was internally false (TSFM context 512 vs classical 1,500; LEAR-lite carries day-of-week) and is replaced by a precise statement of what is and is not equalised. (b) "as its finite-sample guarantee promises" removed — exchangeability is not established for dependent residuals. (c) "ACI reduces to the static construction under no shift" removed — alpha_t keeps updating. (d) The three-level score is renamed a common-grid quantile score; it is **not** CRPS. (e) The value-at-risk object is now defined as the loss of a one-unit long position. (f) COVID and Ukraine are relabelled **retrospective stress tests** because every evaluated model was pretrained after those events; only 2026 is prospective, and a pretraining-contamination limitation is added. (g) The undocumented roll rule of the vendor's continuous futures series is added as a limitation. (h) Brini (2026, arXiv:2607.05291) added — nine TSFMs vs eight econometric benchmarks with an MCS, the closest design to ours, previously missed. | `target journal/01_ESWA_.../ESWA_reviewer_attack_and_revision_memo.md` |
| 2026-08-11 | EnbPI dropped from the conformal battery: it requires a bootstrap ensemble of the underlying model, which cannot be constructed post hoc from cached TSFM forecasts. SPCI-lite (GBR quantile regression on lagged scores) + CQR implemented instead; battery = split-CP, ACI, SPCI-lite, CQR. | conformal.py docstring; unit tests passed (CQR cov .809, SPCI .765 on synthetic) |
| 2026-08-11 | §3.3 fine-tune slot "full FT of TTM (smallest)" replaced by "full FT of Chronos-2" (same `Chronos2Pipeline.fit` API as the LoRA arm; TTM weights not transferred to server). TTM zero-shot/FT can be added later without affecting frozen evaluation rules. Training split honored: train ≤2012-12-31, validation 2013–2014, nothing post-2015. | plan frozen after approval; change is model-roster substitution, not evaluation-rule change |
