__pycache__/
*.py[cod]
.ipynb_checkpoints/
.DS_Store
Thumbs.db
.venv/
venv/
.env

# Never commit: third-party model checkpoints are large and not ours to redistribute.
# The Hugging Face identifiers in README.md are what reproduces.
model_weights/
*.safetensors
*.ckpt
*.bin

# Never commit: publisher PDFs. The exception is our own figures, whose vector
# versions belong in the repository — journals and readers both want them.
*.pdf
!outputs/figures/*.pdf
